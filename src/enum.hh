#ifndef OB_ENUM_HH
#define OB_ENUM_HH

#include <string>
#include <sstream>
#include <iostream>

namespace OB
{

class Enum
{
public:
  Enum();
  ~Enum();

private:

}; // class Enum

} // namespace OB

#endif // OB_ENUM_HH